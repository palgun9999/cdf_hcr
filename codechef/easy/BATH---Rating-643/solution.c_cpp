#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int t;
    cin>>t;
    while(t--)
    {
        int c=0,x,y;
        cin>>x>>y;
        while(x>0 && x>=y*2)
        {
            c+=1;
            x-=y*2;
        }
        cout<<c<<endl;
    }
    return 0;
}
