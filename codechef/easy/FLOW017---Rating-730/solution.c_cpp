#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int t;
    cin>>t;
    while(t--)
    {
        vector<int> sa(3);
        for(int i=0;i<3;i++)
        {
            cin>>sa[i];
        }
        sort(sa.begin(),sa.end());
        cout<<sa[1]<<endl;
    }
    return 0;
}
